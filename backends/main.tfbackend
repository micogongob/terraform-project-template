# NOTE: s3 and dynamodb should be created first
# NOTE: dynamodb table should have the LockID primary key
bucket         = "terraform-project-template-state-us-east-1"
region         = "us-east-1"
dynamodb_table = "terraform-project-template-state-lock"